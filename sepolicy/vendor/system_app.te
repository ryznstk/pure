# Color
hal_client_domain(system_app, vendor_hal_displayfeature_xiaomi)

# Charging Control
allow system_app vendor_sysfs_battery_supply:file rw_file_perms;

# Allow system_app to search the battery directory
allow system_app vendor_sysfs_battery_supply:dir r_dir_perms;
allow system_app vendor_sysfs_battery_supply:file r_file_perms;

# Allow system apps to update thermal configuration
allow system_app sysfs_thermal:file { write open getattr };